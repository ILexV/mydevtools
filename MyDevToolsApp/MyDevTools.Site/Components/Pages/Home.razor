@page "/"
@page "/{lang}"
@using MyDevTools.Site.Resources
@inject NavigationManager Navigation

<MetaTags Title="@AppStrings.Home_Title"
          Description="@AppStrings.Home_Subtitle"
          CurrentUrl="@Navigation.Uri"
          IsHomePage="true"
          Lang="@Lang"
          SiteName="@AppStrings.AppName"/>


<div id="home-page-root">
  <div class="hero mb-8">
    <div class="hero-content flex-col lg:flex-row-reverse gap-8 py-12">
      <img src="/hero_logo.webp" alt="@AppStrings.AppName Hero" class="max-w-sm" width="400" height="297" />
      <div>
        <h1 class="text-5xl font-bold">@AppStrings.AppName</h1>
        <p class="py-6 text-lg">@AppStrings.Home_Subtitle</p>


        @* PWA Install Prompt - Subtle notification *@
        <div id="pwa-install-prompt" class="alert shadow-md bg-base-200 border border-base-300" style="display: none;">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-5 h-5 opacity-70">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <div class="text-sm">
            <div class="font-medium">@AppStrings.PWA_InstallTitle</div>
            <div class="text-xs opacity-70">@AppStrings.PWA_InstallDescription</div>
          </div>
          <div class="flex gap-2">
            <button id="pwa-install-btn" class="btn btn-sm btn-primary">
              @AppStrings.PWA_InstallButton
            </button>
            <button id="pwa-install-dismiss" class="btn btn-sm btn-ghost">
              @AppStrings.PWA_InstallDismiss
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  @* Favorites Section *@
  <div id="favorites-section" class="mb-8">
    <h2 class="text-2xl font-bold mb-4">⭐ @AppStrings.Home_Favorites</h2>
    <div class="favorites-scroll mb-4">
      @foreach (var tool in GetTools())
      {
        <a href="/@Lang/@tool.Slug" class="favorite-tool-compact card card-compact bg-base-200 shadow-xl hover:shadow-2xl hover:scale-105 transition-all duration-200" data-tool-slug="@tool.Slug" style="display: none;">
          <div class="card-body flex-row items-center gap-3">
            <div class="text-3xl">@tool.Icon</div>
            <div class="text-sm font-semibold">@tool.Title</div>
          </div>
        </a>
      }
    </div>
    <div class="favorites-empty border border-base-300 rounded-lg p-4 flex items-center gap-3" style="display: none;">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6 opacity-70"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
      <span class="opacity-70">@AppStrings.Home_NoFavorites</span>
    </div>
  </div>

  @* All Tools Section *@
  <div id="tools">
    <h2 class="text-2xl font-bold mb-4">@AppStrings.Home_FeaturedTools</h2>
    <div class="space-y-6">
      @foreach (var category in GetToolsByCategory())
      {
        @if (category.Tools.Any())
        {
          <div class="divider text-lg divider-info font-semibold text-success">@category.Name</div>

          <div class="tools-grid">
            @foreach (var tool in category.Tools)
            {
              <div class="tool-card-wrapper tooltip tooltip-neutral" data-tip="@tool.Description">
                <a href="/@Lang/@tool.Slug" class="card card-compact bg-base-200 shadow-xl hover:shadow-2xl hover:scale-105 transition-all duration-200 w-full h-full">
                  <div class="card-body items-center">
                    <div class="text-4xl">@tool.Icon</div>
                    <h3 class="card-title text-base text-center">@tool.Title</h3>
                  </div>
                </a>
                <button class="favorite-btn btn btn-ghost btn-circle btn-sm absolute top-2 right-2"
                        data-tool-slug="@tool.Slug"
                        aria-label="@AppStrings.Home_AddToFavorites"
                        aria-pressed="false"
                        title="@AppStrings.Home_AddToFavorites">
                  <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
                  </svg>
                </button>
              </div>
            }
          </div>
        }
      }
    </div>
  </div>
</div>

@code {
  [Parameter]
  public string Lang { get; set; } = "en";

  private List<ToolInfo> GetTools()
  {
    return new List<ToolInfo>
        {
            new("image-converter", "🖼️", AppStrings.ImageConverter_Title, AppStrings.ImageConverter_Description),
            new("image-compressor", "📉", AppStrings.ImageCompressor_Title, AppStrings.ImageCompressor_Description),
            new("image-resizer", "📏", AppStrings.ImageResizer_Title, AppStrings.ImageResizer_Description),
            new("color-converter", "🎨", AppStrings.ColorConverter_Title, AppStrings.ColorConverter_Description),
            new("markdown-preview", "📄", AppStrings.MarkdownPreview_Title, AppStrings.MarkdownPreview_Description),
            new("hash-calculator", "🔐", AppStrings.HashCalculator_Title, AppStrings.HashCalculator_Description),
            new("password-generator", "🎲", AppStrings.PasswordGenerator_Title, AppStrings.PasswordGenerator_Description),
            new("uuid-generator", "🆔", AppStrings.UuidGenerator_Title, AppStrings.UuidGenerator_Description),
            new("lorem-ipsum-generator", "📝", AppStrings.LoremIpsumGenerator_Title, AppStrings.LoremIpsumGenerator_Description),
            new("base64-encoder", "🧬", AppStrings.Base64Encoder_Title, AppStrings.Base64Encoder_Description),
            new("json-beautifier", "✨", AppStrings.JsonBeautifier_Title, AppStrings.JsonBeautifier_Description),
            new("text-case-converter", "🔠", AppStrings.TextCaseConverter_Title, AppStrings.TextCaseConverter_Description),
            new("text-diff-viewer", "📊", AppStrings.TextDiffViewer_Title, AppStrings.TextDiffViewer_Description),
            new("aead-file", "🛡️", AppStrings.Aead_Title, AppStrings.Aead_Description),
            new("openssh-keys", "🔑", AppStrings.OpenSsh_Title, AppStrings.OpenSsh_Description),
            new("x509", "📜", AppStrings.X509_Title, AppStrings.X509_Description),
            new("url-encoder", "🔗", AppStrings.UrlEncoder_Title, AppStrings.UrlEncoder_Description),
            new("xml-beautifier", "🧷", AppStrings.XmlBeautifier_Title, AppStrings.XmlBeautifier_Description),
            new("hex-encoder", "🧱", AppStrings.HexEncoder_Title, AppStrings.HexEncoder_Description),
            new("base32-encoder", "🧪", AppStrings.Base32Encoder_Title, AppStrings.Base32Encoder_Description),
            new("base58-encoder", "🧿", AppStrings.Base58Encoder_Title, AppStrings.Base58Encoder_Description),
            new("date-converter", "📅", AppStrings.DateConverter_Title, AppStrings.DateConverter_Description),
            new("jwt-decoder", "🔓", AppStrings.JwtDecoder_Title, AppStrings.JwtDecoder_Description),
            new("jwt-encoder", "🔐", AppStrings.JwtEncoder_Title, AppStrings.JwtEncoder_Description),
            new("regex-tester", "🔍", AppStrings.RegexTester_Title, AppStrings.RegexTester_Description),
            new("qr-code-generator", "📱", AppStrings.QrCodeGenerator_Title, AppStrings.QrCodeGenerator_Description),
            new("hmac-calculator", "🔐", AppStrings.HmacCalculator_Title, AppStrings.HmacCalculator_Description),
            new("yaml-beautifier-validator", "📄", AppStrings.YamlBeautifierValidator_Title, AppStrings.YamlBeautifierValidator_Description),
            new("qr-scanner", "📷", AppStrings.QrScanner_Title, AppStrings.QrScanner_Description)
        };
  }

  private List<ToolCategory> GetToolsByCategory()
  {
    var tools = GetTools();

    return new List<ToolCategory>
        {
            new(AppStrings.Category_Images, new[]
            {
                tools.First(t => t.Slug == "image-converter"),
                tools.First(t => t.Slug == "image-compressor"),
                tools.First(t => t.Slug == "image-resizer")
            }),
            new(AppStrings.Category_ColorsAndDesign, new[]
            {
                tools.First(t => t.Slug == "color-converter"),
                tools.First(t => t.Slug == "markdown-preview")
            }),
            new(AppStrings.Category_HashingAndSecurity, new[]
            {
                tools.First(t => t.Slug == "hash-calculator"),
                tools.First(t => t.Slug == "password-generator")
            }),
            new(AppStrings.Category_Generators, new[]
            {
                tools.First(t => t.Slug == "uuid-generator"),
                tools.First(t => t.Slug == "lorem-ipsum-generator")
            }),
            new(AppStrings.Category_Encoding, new[]
            {
                tools.First(t => t.Slug == "base64-encoder"),
                tools.First(t => t.Slug == "base32-encoder"),
                tools.First(t => t.Slug == "base58-encoder"),
                tools.First(t => t.Slug == "hex-encoder"),
                tools.First(t => t.Slug == "url-encoder")
            }),
            new(AppStrings.Category_StructuredData, new[]
            {
                tools.First(t => t.Slug == "json-beautifier"),
                tools.First(t => t.Slug == "xml-beautifier"),
                tools.First(t => t.Slug == "yaml-beautifier-validator")
            }),
            new(AppStrings.Category_TextTools, new[]
            {
                tools.First(t => t.Slug == "text-case-converter"),
                tools.First(t => t.Slug == "text-diff-viewer")
            }),
            new(AppStrings.Category_Cryptography, new[]
            {
                tools.First(t => t.Slug == "hmac-calculator"),
                tools.First(t => t.Slug == "aead-file"),
                tools.First(t => t.Slug == "openssh-keys"),
                tools.First(t => t.Slug == "x509")
            }),
            new(AppStrings.Category_DateTime, new[]
            {
                tools.First(t => t.Slug == "date-converter")
            }),
            new(AppStrings.Category_JwtAndTokens, new[]
            {
                tools.First(t => t.Slug == "jwt-decoder"),
                tools.First(t => t.Slug == "jwt-encoder")
            }),
            new(AppStrings.Category_Regex, new[]
            {
                tools.First(t => t.Slug == "regex-tester")
            }),
            new(AppStrings.Category_QrCodes, new[]
            {
                tools.First(t => t.Slug == "qr-code-generator"),
                tools.First(t => t.Slug == "qr-scanner")
            })
        };
  }

  private record ToolInfo(string Slug, string Icon, string Title, string Description);
  private record ToolCategory(string Name, IEnumerable<ToolInfo> Tools);
}