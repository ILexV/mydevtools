---
/**
 * Footer — site chrome. Brand, privacy-first reminder, locale links.
 */
import { LOCALES, type LocaleCode } from "@/registry/locales";
import { localizedPath } from "@/lib/url";
import { t } from "@/i18n/messages";

interface Props {
  locale: LocaleCode;
}

const { locale } = Astro.props as Props;
const brand = t(locale, "common", "AppName");
const privacy = t(locale, "home", "NoDataSent");
const year = new Date().getUTCFullYear();
---

<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-brand">
      <strong>{brand}</strong>
      <p class="privacy">{privacy}</p>
    </div>
    <nav class="footer-langs" aria-label="languages">
      {LOCALES.map((l) => (
        <a hreflang={l.bcp47} href={localizedPath(l.code)}>{l.nativeName}</a>
      ))}
    </nav>
    <p class="copyright">© {year} {brand}</p>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--mdt-border-soft);
    background: var(--mdt-surface-muted);
    margin-top: 0;
  }
  .footer-inner {
    max-width: var(--mdt-content-max);
    margin: 0 auto;
    padding: 1.75rem 1.25rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .footer-brand strong { font-size: 0.95rem; letter-spacing: -0.01em; }
  .privacy {
    color: var(--mdt-text-faint);
    font-size: var(--mdt-fs-meta);
    margin-top: 0.2rem;
    max-width: 42ch;
  }
  .footer-langs {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.85rem;
    margin-left: auto;
  }
  .footer-langs a {
    color: var(--mdt-text-muted);
    text-decoration: none;
    font-size: var(--mdt-fs-meta);
  }
  .footer-langs a:hover { color: var(--mdt-accent); }
  .copyright {
    color: var(--mdt-text-faint);
    font-family: var(--mdt-font-mono);
    font-size: var(--mdt-fs-micro);
    width: 100%;
  }
</style>
