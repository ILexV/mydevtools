services:
  mydevtools-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mydevtools-app
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always
